# Frontend

Фронтенд живет в `frontend/`, но команды запускаются из корня монорепо.

## Запуск

```bash
npm run dev
```

## Сборка

```bash
npm run build
```

## ENV

Локальные переменные фронтенда задаются через `config/env/local/.env.frontend.local`.
Шаблон: `frontend/.env.example`.
